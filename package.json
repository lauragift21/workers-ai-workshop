{
  "name": "workers-ai-workshop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --remote",
    "start": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^3.15.0"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.23"
  }
}
